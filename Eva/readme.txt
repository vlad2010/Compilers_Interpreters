to run it type:
node eva.js



// more things TODO : 
1. (export <name1> <name2>) - export section for modules, allows to hide some methods
2. (import (export1, export2, ... ) name)  - import only selected symbols from module

3. Other data structures
4. Prototype class inheritance using object keyword

(var base (object (value 100)))
(object (x 10)(y 20)(__proto base))  - object expression

5. list and arrays 
(var values (list 42 "hello" foo))

(prop values 0)
(prop values 1)

(idx values 0)  // 42
(idx values 1)  // "Hello"


